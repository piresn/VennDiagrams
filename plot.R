try(venner(values$X,
           weighted = values$weights,
           fontsize = as.numeric(values$fontsize),
           labelsize = as.numeric(values$labelsize),
           lwd = as.numeric(values$lwd),
           lwcol = values$lwcol,
           lbcol = values$lbcol,
           fontcol = values$fontcol,
           lty = values$lty,
           inters1 = values$inters1,
           inters2 = values$inters2,
           colorify = values$colorify,
           cols = values$cols)
)